import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Neon serverless driver works in edge and Node runtimes without special config.
  // Do not add experimental features here without justification.
};

export default nextConfig;
